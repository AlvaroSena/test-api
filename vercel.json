{
  "version": 2,
  "builds": [
    {
      "src": "./src/app.js",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [{ "source": "/api/v1/(.*)", "destination": "/api/v1/" }],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}